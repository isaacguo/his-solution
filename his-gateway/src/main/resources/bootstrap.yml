spring:
  application:
    name: HisGateway
  cloud:
    config:
      uri: http://HisConfig:17601

server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://HisEureka:8761/eureka/
