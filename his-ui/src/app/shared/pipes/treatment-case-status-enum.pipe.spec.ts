import { TreatmentCaseStatusEnumPipe } from './treatment-case-status-enum.pipe';

describe('TreatmentCaseStatusEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new TreatmentCaseStatusEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
